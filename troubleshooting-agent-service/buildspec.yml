version: 0.2

# CodeBuild buildspec for Troubleshooting Agent Service
# Purpose: Package Lambda function with dependencies

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "Installing dependencies..."
      - pip install --upgrade pip
      
  pre_build:
    commands:
      - echo "Pre-build phase started on `date`"
      - echo "Creating deployment package..."
      
  build:
    commands:
      - echo "Build phase started on `date`"
      
      # Create deployment directory
      - mkdir -p deployment
      
      # Install dependencies to deployment directory
      - pip install -r troubleshooting-agent-service/requirements.txt -t deployment/
      
      # Copy Lambda handler code
      - cp -r troubleshooting-agent-service/src/troubleshooting_handler/* deployment/
      
      # Create deployment package
      - cd deployment
      - zip -r ../troubleshooting-agent-lambda.zip .
      - cd ..
      
      - echo "Deployment package created troubleshooting-agent-lambda.zip"
      - ls -lh troubleshooting-agent-lambda.zip
      
  post_build:
    commands:
      - echo "Build completed on `date`"

artifacts:
  files:
    - troubleshooting-agent-lambda.zip
    - terraform/troubleshooting-agent-service/**/*
  name: troubleshooting-agent-build-$(date +%Y%m%d-%H%M%S)

cache:
  paths:
    - '/root/.cache/pip/**/*'
