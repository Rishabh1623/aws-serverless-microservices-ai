# AWS Strands Agents SDK (Official AWS open-source)
strands-agents>=0.1.0

# Anthropic SDK for direct API access (bypasses Bedrock billing issues)
anthropic>=0.39.0

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# HTTP requests for calling microservice APIs
requests>=2.31.0

# JSON handling
python-json-logger>=2.0.7

# OpenTelemetry - Fixed for Python 3.11 compatibility (PEP 479)
opentelemetry-api>=1.30.0,<2.0.0
opentelemetry-sdk>=1.30.0,<2.0.0
opentelemetry-instrumentation-threading>=0.51b0,<1.0.0
